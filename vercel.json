{
  "buildCommand": "cd ject && npm install && npm run build && cd .. && mkdir -p public && cp -r ject/dist/* public/",
  "outputDirectory": "public",
  "regions": ["iad1"],
  "env": {
    "REACT_APP_API_URL": "@react_app_api_url"
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
